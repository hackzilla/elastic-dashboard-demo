{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ page }}</h1>
    <ul>
        {% for path in paths %}
            <li><a href="{{ url('html', {path: path}) }}">{% if path %}{{ path }}{% else %}homepage{% endif %}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
